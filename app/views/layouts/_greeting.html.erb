<div id="greeter-box">
	<% if current_user %>
		Logged in as: <%= current_user.email %>
		<%= button_to "Log Out", destroy_user_session_url, method: :delete%>
	<% else %>
		<%= link_to 'Sign In', new_user_session_url %> | <%= link_to 'Sign Up', new_user_registration_url %>
	<% end %>
</div>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Submitter</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% questions.each do |question| %>
      <tr>
        <td><strong><%= link_to question.title, question %></strong></td>
        <td><%= question.user.email %></td>
        <% if current_user && current_user.owns?(question) %>
          <td><%= link_to 'Edit', edit_question_path(question) %></td>
          <td><%= link_to 'Delete', question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
